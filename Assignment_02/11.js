//Write a code that uses a lambda function to calculate the square of given numbers.

const numbers = [2,4,6,8,10];

const squaredNumbers = numbers.map(num => num * num);
console.log(squaredNumbers);
