// Write a code that user a lambda function to calculate the square of a given numbers.
function square(num){
    no = num * num;
    return no;
}
const number = 5;
let result = square(number);
console.log(`The square of ${number} in ${result}`);
